interface:
  display_name: "Thuyet Minh Offline Gpu"
  short_description: "Repo-specific patterns and workflows for thuyet-minh-offline-gpu"
  default_prompt: "Use the thuyet-minh-offline-gpu repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true